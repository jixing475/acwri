% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/docx-revisions.R
\name{extract_docx_revisions}
\alias{extract_docx_revisions}
\title{Extract Track Changes from Word Document}
\usage{
extract_docx_revisions(
  docx_path,
  output = NULL,
  python_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{docx_path}{Path to the Word document to process.}

\item{output}{Optional path to the output Markdown file. If not specified,
the function returns a character vector.}

\item{python_path}{Path to the Python executable. Defaults to the bundled
virtual environment if it exists, otherwise searches for system python3.}

\item{quiet}{Whether to run silently. Defaults to \code{FALSE}.}
}
\value{
If \code{output} is specified, returns \code{NULL} (invisibly);
otherwise returns a character vector containing the Markdown content.
}
\description{
Extract all track changes (revisions) from a Word document (.docx), including
comments, deletions, insertions, formatting changes, and paragraph changes,
and output in Markdown format.

This function wraps the Python script \code{extract_docx_track_changes.py} and
requires the user to configure a Python environment with the \code{docx2python}
package installed.
}
\section{Python Environment Setup}{

This function depends on the Python package \code{docx2python}. Setup steps:

\if{html}{\out{<div class="sourceCode bash">}}\preformatted{# In the inst/docx2md directory
uv venv .venv --python 3.12
source .venv/bin/activate
uv pip install docx2python
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Return character vector
result <- extract_docx_revisions("manuscript.docx")
cat(result, sep = "\n")

# Save to file
extract_docx_revisions("manuscript.docx", output = "changes.md")

# Use custom Python path
extract_docx_revisions(
  "manuscript.docx",
  python_path = "/usr/bin/python3"
)
}

}
